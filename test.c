#include <stdio.h>

int main() {

    int x = 5;
    int y = 3;
    float z = 0;

    //z = x + y;
    //z = x - (2*y);
    //z = (float)x / (float)y;
    //z = x % y;

    //Augmented assignment operators
    // x += 3;
    // x -= 4;
    // x *= 2;
    // x /= 3;
    // x %= 2;

    printf("%.6f\n", z);
}